class Blog {
    constructor( id, userId, title, content ) {
            this.blogId = id;
            this.userId = userId;
            this.title = title;
            this.content = content;            
    }
}

module.exports = Blog;